import torch
import torchvision
from torch.utils.data import Dataset,DataLoader # Classes to work with data

import numpy as np
import math

# In task we need to predict wine category
class wine_dateset(Dataset):

    def __init__(self) -> None:
        # data loading
    
    def __getitem__(self, index):
        #dataset[0]
    
    def __len__(self):

        #get len of ctg

